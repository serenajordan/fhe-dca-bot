{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@10.0.0 --activate && pnpm -w install --no-frozen-lockfile",
  "buildCommand": "pnpm -C app/packages/site build",
  "outputDirectory": "app/packages/site/.next"
}

{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "pnpm build:site",
  "installCommand": "corepack enable && corepack prepare pnpm@10.0.0 --activate && ./scripts/vercel-install.sh",
  "outputDirectory": "app/packages/site/.next"
}

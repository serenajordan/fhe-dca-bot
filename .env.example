# Network Configuration
SEPOLIA_RPC_URL="" # Sepolia RPC URL (e.g., https://sepolia.infura.io/v3/YOUR_PROJECT_ID)
PRIVATE_KEY="" # never commit real key

# Token Configuration (for localhost deployment)
TOKEN_IN="" # Input token address (e.g., USDC)
TOKEN_OUT="" # Output token address (e.g., ETH)
ROUTER="" # DEX router address (e.g., Uniswap V2 Router)

# Keeper Configuration
KEEPER_FEE_BPS="10" # Keeper fee in basis points (10 = 0.10%)

# K-Anonymity Configuration
K_MIN="10" # Minimum users for k-anonymity (10 for production, 3 for localhost)
TIME_WINDOW="900" # Time window in seconds (900 = 15min for production, 60 for localhost)

# Optional: Contract addresses (auto-populated by deploy script)
# EXECUTOR_ADDRESS=""
# INTENTS_ADDRESS=""
# AGGREGATOR_ADDRESS=""
# ADAPTER_ADDRESS=""

# API Keys (optional)
INFURA_API_KEY="" # Infura API key (alternative to SEPOLIA_RPC_URL)
ETHERSCAN_API_KEY="" # Etherscan API key for contract verification
